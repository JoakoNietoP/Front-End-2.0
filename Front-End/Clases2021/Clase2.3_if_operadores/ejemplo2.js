const nacionalidad = "brasilero";

console.log(
  nacionalidad === "argentino"
    ? "tu documento empieza en 32"
    : "tu documento empieza en 92"
);

//* ternario es para casos que son VERDAD o MENTIRA

const helado = true;

console.log(helado ? "siii" : "buuuu");

const login = true;

console.log(login ? "redirigit a cart" : "redirigir a login");
