//El ternario es otro tipo de if, no tiene else ifs. Solo dos cosas pueden pasar

const ladra = true;

//condicion === Qué la hace true? que hace si es true : que hace si es false

//! coindicion ? true : false
console.log(ladra === true ? "es un perro" : "no es un perro");

//* el ternario no permite no tener que hace en caso de false

const cucharas = 16;
console.log(cucharas > 15 ? "nos alcanza" : "no alcanza");
